// Package version contains linker-injected build metadata.
package version

// Version is the application version.
var Version = "dev"

// Commit is the source revision used for the build.
var Commit = "unknown"

// BuildDate is the commit date used for reproducible build metadata.
var BuildDate = "unknown"
